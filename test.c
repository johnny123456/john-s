#include<stdio.h>
#include<stdlib.h>

int main(int argc ,char * argv[])
{
	/*char * buf;
	buf=argv[1];
	printf("buf is %s\n",buf);
	printf("*buf is %ld\n",*buf);
	printf("&buf is %ld\n",&buf);*/

	char a[]={'a','b'};
	printf("a is %s\n",a);
	return 0;
}
